const fs=require('fs'); const path='src/services/schedule.service.js'; const s=fs.readFileSync(path,'utf8'); const counts={'{':0,'}':0,'(':0,')':0,'`':0,"'":0,'"':0}; for(const ch of s){ if(counts.hasOwnProperty(ch)) counts[ch]++; } console.log('counts',counts); const lines=s.split('\n'); console.log('lines',lines.length); for(let i=1;i<=lines.length;i++){ if(i>=1100 && i<=1145) console.log(i, lines[i-1]); }