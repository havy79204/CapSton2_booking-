const fs=require('fs'); const s=fs.readFileSync('src/services/schedule.service.js','utf8'); const lines=s.split('\n'); let bal=0; for(let i=0;i<lines.length;i++){ const line=lines[i]; for(const ch of line){ if(ch==='{') { bal++; console.log('inc', i+1, line.trim(), 'bal=', bal);} if(ch==='}') { bal--; console.log('dec', i+1, line.trim(), 'bal=', bal);} } if(bal<0) { console.log('Negative balance at line', i+1); break; } } console.log('final balance', bal); for(let i=0;i<lines.length;i++){ const ln=lines[i]; if(ln.includes('async function')||ln.includes('function ')||ln.includes('module.exports')) console.log(i+1, ln.trim()); }